import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#9a044c", // Schimbă culoarea primară aici
    },
    secondary: {
      main: "#802c80", // Schimbă culoarea secundară aici
    },
  },
});

export default theme;
